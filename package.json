{
  "scripts": {
    "start": "npm install && concurrently \"npm run startServer\" \"npm run startClient\"",
    "startClient": "cd client && npm install && npm run dev",
    "startServer": "cd server && npm install && node server.js"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "react-barcode-reader": "^0.0.2",
    "react-qr-barcode-scanner": "^2.0.0"
  },
  "name": "react-barcode-reader",
  "version": "1.0.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  }
}
